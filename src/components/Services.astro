---

import { services } from '../data/services';

---
<section id="services">
  <div class="services-header reveal">
    <div>
      <div class="section-label">What We Do</div>
      <h2 class="section-title">Our Core<br /><span>Services</span></h2>
    </div>
    <p class="services-desc">From television production to digital content and full-scale event coverage, we deliver world-class media for Uganda, East Africa, and beyond.</p>
  </div>

  <div class="services-grid">
    {services.map((s, i) => (
      <a href={`/services/${s.slug}`} class={`service-card reveal stagger-${(i % 2) + 1}`}>
        <span class="service-num">{String(i + 1).padStart(2, '0')}</span>
        <div class="service-icon">
          <svg width="24" height="24" fill="none" stroke="var(--color-green)" stroke-width="2" viewBox="0 0 24 24" set:html={s.icon}></svg>
        </div>
        <div class="service-body">
          <div class="service-name">{s.name}</div>
          <p class="service-text">{s.blurb}</p>
        </div>
      </a>
    ))}
  </div>
</section>

<style>
  #services { padding: 120px 8% 100px; background: #F5F7FA; }

  .services-header {
    display: flex;
    align-items: flex-end;
    justify-content: space-between;
    margin-bottom: 56px;
    gap: 40px;
  }
  .services-header .section-label { color: var(--color-orange); }
  .services-header .section-label::before { background: var(--color-orange); }
  .services-desc {
    font-size: 0.95rem;
    color: var(--color-text-muted);
    max-width: 360px;
    line-height: 1.75;
    font-weight: 300;
  }

  .services-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 20px;
  }
  .service-card {
    position: relative;
    display: flex;
    gap: 22px;
    align-items: flex-start;
    background: #1A5FA8;
    border: 1px solid rgba(255,255,255,0.08);
    border-radius: 16px;
    padding: 28px 30px;
    overflow: hidden;
    text-decoration: none;
    color: inherit;
    transition: transform 0.3s, box-shadow 0.3s, border-color 0.3s;
  }
  .service-card::before {
    content: '';
    position: absolute;
    top: 0; bottom: 0; left: 0;
    width: 3px;
    background: linear-gradient(180deg, var(--color-green-light), var(--color-orange));
    transform: scaleY(0);
    transform-origin: top;
    transition: transform 0.4s ease;
  }
  .service-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 16px 40px rgba(26,95,168,0.35);
    border-color: rgba(255,255,255,0.22);
  }
  .service-card:hover::before { transform: scaleY(1); }
  .service-icon {
    width: 54px; height: 54px;
    flex-shrink: 0;
    background: rgba(255,255,255,0.1);
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: transform 0.3s, background 0.3s;
    --color-green: var(--color-green-light);
  }
  .service-card:hover .service-icon { transform: scale(1.06); background: rgba(255,255,255,0.16); }
  .service-body { flex: 1; }
  .service-name {
    font-family: var(--font-heading);
    font-weight: 700;
    font-size: 1.25rem;
    color: #ffffff;
    text-transform: uppercase;
    letter-spacing: 0.03em;
    margin-bottom: 0.4rem;
    line-height: 1.1;
  }
  .service-text {
    font-size: 0.88rem;
    color: rgba(255,255,255,0.62);
    line-height: 1.7;
    font-weight: 300;
  }
  .service-num {
    position: absolute;
    top: 16px; right: 22px;
    font-family: var(--font-heading);
    font-size: 2.2rem;
    font-weight: 800;
    color: rgba(255,255,255,0.07);
    line-height: 1;
    letter-spacing: -0.02em;
  }

  @media (max-width: 900px) {
    .services-grid { grid-template-columns: 1fr; }
  }
</style>
